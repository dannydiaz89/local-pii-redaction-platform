# generated by datamodel-codegen:
#   filename:  detect-request.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Literal
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, confloat, conint, constr


class Chunk(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    id: constr(min_length=1, max_length=100, strict=True)
    text: constr(min_length=1, max_length=20000, strict=True)
    absoluteStart: conint(ge=0, strict=True)
    language: constr(pattern=r'^[a-z]{2,3}(?:-[A-Z]{2})?$', strict=True) | None = None


class Options(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    maxDetectionsPerChunk: conint(ge=1, le=1000, strict=True)


class EntityType(StrEnum):
    PERSON = 'PERSON'
    EMAIL = 'EMAIL'
    PHONE = 'PHONE'
    ADDRESS = 'ADDRESS'
    LOCATION = 'LOCATION'
    ORGANIZATION = 'ORGANIZATION'
    DATE_OF_BIRTH = 'DATE_OF_BIRTH'
    SSN = 'SSN'
    NATIONAL_ID = 'NATIONAL_ID'
    PASSPORT = 'PASSPORT'
    DRIVER_LICENSE = 'DRIVER_LICENSE'
    CREDIT_CARD = 'CREDIT_CARD'
    BANK_ACCOUNT = 'BANK_ACCOUNT'
    ROUTING_NUMBER = 'ROUTING_NUMBER'
    MEDICAL_RECORD = 'MEDICAL_RECORD'
    HEALTH_PLAN_ID = 'HEALTH_PLAN_ID'
    ACCOUNT_ID = 'ACCOUNT_ID'
    USERNAME = 'USERNAME'
    IP_ADDRESS = 'IP_ADDRESS'
    MAC_ADDRESS = 'MAC_ADDRESS'
    API_KEY = 'API_KEY'
    ACCESS_TOKEN = 'ACCESS_TOKEN'
    PASSWORD = 'PASSWORD'
    CUSTOM = 'CUSTOM'


class InferenceDetectRequest(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schemaVersion: Literal['1.0.0']
    requestId: UUID
    chunks: list[Chunk] = Field(..., max_length=64, min_length=1)
    entityTypes: list[EntityType] = Field(..., min_length=1)
    minimumConfidence: confloat(ge=0.0, le=1.0, strict=True)
    options: Options
